You are an expert DevOps and Cloud Platform Engineer.

Your task is to generate a COMPLETE, WORKING multi-cloud deployment automation system with the following requirements:

GOAL:
Build a user-assisted cloud infrastructure deployment system that works across AWS, Azure, and GCP using Terraform and a wrapper script.

CORE REQUIREMENTS:
1. Use Terraform as the infrastructure-as-code tool.
2. Support AWS, Azure, and GCP as selectable cloud providers.
3. The user must be interactively prompted to:
   - Choose cloud provider (aws / azure / gcp)
   - Choose region
   - Choose instance size
4. The system must automatically deploy:
   - Networking (basic VPC / VNet / VPC equivalent)
   - One compute instance (VM)
   - Security rules to allow SSH access
5. Authentication must rely on existing CLI logins:
   - AWS CLI
   - Azure CLI
   - GCP gcloud
   (Do NOT hardcode credentials.)

PROJECT STRUCTURE:
Generate the following directory structure:

cloud-deploy/
├── deploy.sh
├── main.tf
├── variables.tf
├── outputs.tf
├── aws/
│   ├── main.tf
│   ├── variables.tf
├── azure/
│   ├── main.tf
│   ├── variables.tf
├── gcp/
│   ├── main.tf
│   ├── variables.tf

IMPLEMENTATION DETAILS:
- deploy.sh must:
  - Prompt the user for inputs
  - Export Terraform variables
  - Run terraform init and terraform apply automatically
- Terraform must:
  - Dynamically load the correct cloud module based on user input
  - Use minimal but valid configurations
  - Follow best practices
- Code must be syntactically correct and runnable
- Avoid placeholders unless unavoidable; if used, clearly comment them

OUTPUT FORMAT:
1. Show the full directory tree
2. Then show the complete content of EACH file
3. Add concise comments explaining key decisions
4. Do NOT omit any files
5. Do NOT summarize — generate full code

QUALITY BAR:
- Treat this as production-ready starter infrastructure
- Assume the user is technical
- Be precise, explicit, and robust
